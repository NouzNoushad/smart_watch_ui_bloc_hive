import 'package:flutter/material.dart';

class ColorPicker {
  static const Color seedColor = Colors.deepPurple;
  static const Color whiteColor = Colors.white;
  static const Color blackColor = Colors.black;
  static const Color greyColor = Colors.grey;
  static const Color transparent = Colors.transparent;
  static const Color lightColor = Colors.white24;
  static Color lightLikeColor = Colors.red.withOpacity(0.2);
  static const Color likeColor = Colors.red;
  static const Color tealColor = Colors.teal;
}
